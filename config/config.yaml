symbol: "BTC-USD"
interval: "1h"
lookback_days: 365
strategy:
  name: "sma_crossover"
  params:
    short: 20
    long: 50
risk:
  position_fraction: 1.0    # im MVP nur Lernzwecke
  max_drawdown_alert: 0.2   # 20% Warnschwelle
run:
  live_interval_sec: 300
